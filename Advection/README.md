# Advection

```bash
python atm.py
python random_lst.py
```

Conditions and characteristic reference solutions are generated by
`reference_data.py`. The experiment configuration is defined in `config.py` and
`protocol.json`. Outputs are written under `results/`.
