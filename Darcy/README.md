# Darcy

```bash
python atm.py
python random_lst.py
```

Permeability conditions are represented on a 61 x 61 uniform grid. Relative L2
errors are evaluated against finite-difference reference solutions on a 241 x
241 uniform grid. The experiment configuration is defined in `config.py` and
`protocol.json`. Outputs are written under `results/`.
